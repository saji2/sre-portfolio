apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: app-production
  labels:
    app: frontend-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: frontend-service
