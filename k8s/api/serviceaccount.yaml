#------------------------------------------------------------------------------
# ServiceAccount for API Service
# NOTE: Update the IAM role ARN after Terraform deployment
#------------------------------------------------------------------------------
apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-service
  namespace: app-production
  annotations:
    # Replace with actual IAM role ARN from terraform output
    eks.amazonaws.com/role-arn: "${API_SERVICE_ROLE_ARN}"
